name: 'test-post-if-condition'
description: ''
inputs:
  always-run-post:
    description: 'Always run the post step'
    required: false
    default: false
runs:
  using: 'node12'
  main: 'dist/main/index.js'
  post: 'dist/post/index.js'
  post-if: success() || (with.always-run-post == 'true' && always()) || (with == 'true' && always()) || (with.ALWAYS_RUN_POST == 'true' && always()) || (with.INPUT_ALWAYS_RUN_POST == 'true' && always())
